{
  "machine_id": "M272",
  "schema": "spintype-native/1",
  "modes": [
    1
  ],
  "inherits_from": null,
  "spin_types": {
    "140": {
      "role": "paid_spin",
      "play": "NormalCollectionSpin",
      "economy": {
        "win_field": "WinCredits",
        "kind": "real"
      },
      "signature": [
        "BetAmount",
        "CostCredits",
        "StopSymbolsByCol",
        "PayoutByPayline",
        "WinCredits"
      ],
      "observed_fields": [
        "AccCredits",
        "BetAmount",
        "CollectCount",
        "CostCredits",
        "CreditsSymbols",
        "CurJackpotStoreWin",
        "GameplayTriggerType",
        "IsLackCreditsSpin",
        "LastCredits",
        "PayLineGroupId",
        "PayoutByPayline",
        "PayoutGroupId",
        "PayoutIdToWinAmount",
        "RTPId",
        "ReMarks",
        "ReelSkin",
        "RewardLastNode",
        "SpinTimes",
        "SpinType",
        "StopSymbolsByCol",
        "SymbolIndexToRewards",
        "WinCredits"
      ],
      "observed_field_presence": {
        "AccCredits": 1.0,
        "BetAmount": 1.0,
        "CollectCount": 1.0,
        "CostCredits": 1.0,
        "CreditsSymbols": 1.0,
        "CurJackpotStoreWin": 1.0,
        "GameplayTriggerType": 1.0,
        "IsLackCreditsSpin": 1.0,
        "LastCredits": 1.0,
        "PayLineGroupId": 1.0,
        "PayoutByPayline": 1.0,
        "PayoutGroupId": 1.0,
        "PayoutIdToWinAmount": 1.0,
        "RTPId": 1.0,
        "ReMarks": 1.0,
        "ReelSkin": 1.0,
        "RewardLastNode": 1.0,
        "SpinTimes": 1.0,
        "SpinType": 1.0,
        "StopSymbolsByCol": 1.0,
        "SymbolIndexToRewards": 1.0,
        "WinCredits": 1.0
      },
      "observed_fields_source": "st_inventory M272/mode_1 bucket 4876fcbd/4140b23f 2026-06-18"
    },
    "126": {
      "role": "freespin",
      "play": "NewFreespin",
      "economy": {
        "win_field": "WinCredits",
        "kind": "real"
      },
      "signature": [
        "BetAmount",
        "CostCredits",
        "StopSymbolsByCol",
        "PayoutByPayline",
        "PayoutIdToWinAmount",
        "RewardLastNode",
        "ReelSkin",
        "ReMarks",
        "ExtraRatio",
        "GameplayTriggerType",
        "WinCredits"
      ],
      "observed_fields": [
        "BetAmount",
        "CostCredits",
        "CurJackpotStoreWin",
        "ExtraRatio",
        "GameplayTriggerType",
        "IsLackCreditsSpin",
        "LastCredits",
        "PayLineGroupId",
        "PayoutByPayline",
        "PayoutGroupId",
        "PayoutIdToWinAmount",
        "RTPId",
        "ReMarks",
        "ReelSkin",
        "RewardLastNode",
        "SpinTimes",
        "SpinType",
        "StopSymbolsByCol",
        "WinCredits"
      ],
      "observed_field_presence": {
        "BetAmount": 1.0,
        "CostCredits": 1.0,
        "CurJackpotStoreWin": 1.0,
        "ExtraRatio": 1.0,
        "GameplayTriggerType": 1.0,
        "IsLackCreditsSpin": 1.0,
        "LastCredits": 1.0,
        "PayLineGroupId": 1.0,
        "PayoutByPayline": 1.0,
        "PayoutGroupId": 1.0,
        "PayoutIdToWinAmount": 1.0,
        "RTPId": 1.0,
        "ReMarks": 1.0,
        "ReelSkin": 1.0,
        "RewardLastNode": 1.0,
        "SpinTimes": 1.0,
        "SpinType": 1.0,
        "StopSymbolsByCol": 1.0,
        "WinCredits": 1.0
      },
      "observed_fields_source": "st_inventory M272/mode_1 bucket 4876fcbd/4140b23f 2026-06-18",
      "trigger_paths": {
        "fallback": {
          "kind": "trigger_anchor_walk",
          "note": "GameplayTriggerType does NOT discriminate on M272 (constant 0 on 474/474 ST126 entry rounds — unlike M275 where {0,2}). Discrimination is by the opening paid ST140 round's anchor: scatter = pid 666 present with win 0 (434/474), collect_peak = CollectCount==1000 at the opener (40/474). The two anchors are mutually exclusive and exhaustive over all 474 trigger entries (434 scatter + 40 collect-peak, 0 overlap, 0 orphans). No round_field discriminator declared, so the trigger_path extractor uses the trigger_anchor_walk over paths.opened_by."
        },
        "paths": {
          "scatter": {
            "opened_by": {
              "payout_id": "666"
            },
            "label": "scatter (3 bonus symbols)"
          },
          "collect_peak": {
            "opened_by": {
              "counter": "CollectCount",
              "at_peak": 1000
            },
            "label": "collect-peak (pity timer)"
          }
        },
        "multi_trigger_policy": "additive_sessions"
      }
    }
  },
  "trigger": {
    "payout_id": "666",
    "remarks": "Freespin",
    "opens": "NewFreespin",
    "note": "TWO trigger paths open the SAME NewFreespin session from the paid base ST140: (a) scatter pid 666 = bonus symbols on the paid spin (win 0 marker; 434/474 of all triggers); (b) CollectCount==1000 (deterministic pity timer, +1 every paid spin, resets at 1000; 40/474). The two are mutually exclusive over all 474 observed trigger entries (0 overlap, 0 orphans). M272's GameplayTriggerType is constant 0 on every ST126 round (does NOT discriminate the paths, unlike M275); the per-round anchor walk (pid-666 vs CollectCount-reset on the opening ST140) discriminates them exactly. ST126 (NewFreespin) carries WinCredits>0 on 474 rounds (19,180,000, ~48% of all in-sample payback) but NO PayoutIdToWinAmount (PayoutIdToWinAmount={} on 7608/7608, PayoutByPayline empty on 7608/7608) — divergent from M275/M182 ST126 which DO carry a natural payid; M272's freespin win therefore needs a config-only SynthesizePayIdRule (st126) to keep sum(payid)==summary and _unattributed_st126==0. CurJackpotStoreWin present and =0 on 100% (fleet test-interface zeroing)."
  },
  "validation": {
    "status": "auto",
    "user_signed_off": false,
    "confirmed_against": {
      "mode": 1,
      "rawdata": "M272/mode_1"
    },
    "evidence": "st_inventory M272/mode_1 (single chunk chunk_0002.json, 8 robots, 47,608 rounds, _cache_version 3 REAL, _dev_sample null, single md5 bucket 4876fcbd9d99.../4140b23fb686..., bet=1000): SpinType tally {140:40000, 126:7608}. ST140 role paid_spin/NormalCollectionSpin (StopSymbolsByCol + PayoutByPayline + PayoutIdToWinAmount + WinCredits on 100%, CostCredits=BetAmount=1000 on 100%; ReMarks empty on 40,000/40,000); observed_fields BYTE-IDENTICAL to M186/M153 ST140 -> STRICT-REUSE the shared paid_spin plugins (no new code). ST126 role freespin/NewFreespin (CostCredits=0 FREE on 100%, ExtraRatio populated {100..600}, ReMarks 'Freespin N; CollectCount:..; AddCollectCount:..; ExtraRatio:..'); observed_fields BYTE-IDENTICAL to M275 ST126 -> STRICT-REUSE freespin_dynamics (no new code). Sequence: ST140->ST126 474 (freespin triggers), ST126->ST126 7134 (chains), ST126->ST140 466 (exits); ~16 freespins/session. Trigger anchor split: 434 scatter (pid 666, not at peak) + 40 collect-peak (CollectCount==1000, no pid 666), perfectly partitioned. ATTRIBUTION DIVERGENCE: ST126 carries WinCredits>0 on 474 rounds (total 19,180,000) with PayoutIdToWinAmount={} on 100% -> needs config-only SynthesizePayIdRule st126 (coordinator-added); without it the freespin win lands in _unattributed_st126 and the gate fails (EXPECTED pre-rule). CurJackpotStoreWin=0 on 100% (fleet test-interface zeroing). NOT user-signed-off (validation.status='auto').",
    "caveats": [
      "T1 manifest-only onboard, multi-ST (paid base ST140 NormalCollectionSpin + freespin ST126 NewFreespin). STRICT-REUSE of the shared paid_spin plugins (ST140) and freespin_dynamics (ST126); NO new code, NO fork, NO closure change. base_hash 3ddaa183f38c unchanged before/after (manifest is base-excluded).",
      "ATTRIBUTION: ST126 (NewFreespin settlement) has NO native PayoutIdToWinAmount on M272 (={} on 7608/7608, win on 474), DIVERGENT from the reference ST126 (M275/M182 ST126 DO carry a natural per-round payid). This is an attribution case, NOT a signature mismatch: the field key is present (in the signature/observed_fields) but empty-valued. Needs a config-only SynthesizePayIdRule (st126, label_format=spin_type) so sum(payid)==summary and _unattributed_st126->0 (EXACT M43/M279/M44/M283 precedent applied to a freespin settlement). The rule is added by the COORDINATOR to configs/machine_round_win_rules.json (returned in synth_rules to avoid a write race); status='needs_synth' until applied.",
      "trigger_paths: TWO paths (scatter pid 666 + collect_peak CollectCount==1000), mirroring M275 — but the M275 GameplayTriggerType round_field discriminator does NOT work on M272 (GTT constant 0 on all ST126 rounds). Declared WITHOUT a round_field discriminator so the trigger_path extractor uses the trigger_anchor_walk over paths.opened_by (pid-666 anchor vs CollectCount-reset). Data supports the split exactly (434 scatter + 40 collect-peak; 0 overlap, 0 orphans over 474 entries).",
      "CurJackpotStoreWin present and =0 on 100% of 47,608 records (established fleet-wide test-interface zeroing; see out_of_engine_mechanics). RTP excludes any progressive.",
      "GameplayTriggerType present and constant 0 on all rounds in mode 1 (this machine emits it; non-zero meaning undetermined from mode-1 data). Parse-as-is (constant 0); forward-compatible. It is in the ST126 signature (matching M275) but is NOT used as a trigger-path discriminator here (it does not discriminate on M272).",
      "Collect-mechanic fields on ST140 (CollectCount 1..1000 cycling, AccCredits, SymbolIndexToRewards, CreditsSymbols) are present and ACTIVE: CollectCount is the pity-timer metronome that drives one of the two freespin trigger paths (CC==1000). Surfaced as the collect_peak trigger path; the per-round collect map / per-cycle un-merge remains parser_blind (per-ST extraction layer, framework-team).",
      "Single config-md5 bucket 4876fcbd/4140b23f (single chunk chunk_0002.json; no mixed-md5 pooling concern).",
      "Modes 2/5/7 NOT analyzed (mode-1-only onboarding). Mode dirs were not opened; they may exercise other STs or non-zero GameplayTriggerType and MUST get their own W1 + reuse pass before any reuse claim. validation.status stays 'auto' until breaker + gate-7 render verify + user sign-off."
    ],
    "date": "2026-06-18"
  },
  "out_of_engine_mechanics": [
    {
      "name": "CurJackpotStoreWin progressive (test-interface-zeroed)",
      "desc": "Field 'CurJackpotStoreWin' is present on every ST140 and ST126 round but =0 on 100% of 47,608 records. Established fleet-wide test-interface zeroing, declaration-confirmed (memory: gate-8 ruling + M279/M283/M275 sign-off — a real progressive jackpot store the current testspin interface forces to 0). Parse as-is at 0; the field is already in both ST signatures so parsing is FORWARD-COMPATIBLE (when the interface populates it the existing parser picks it up unchanged). The reported RTP excludes the progressive; it would add on top once the interface enables it. NOT a per-machine investigation — fleet pattern, one-time recorded.",
      "rtp_impact": true,
      "rtp_impact_current": 0,
      "source": "domain",
      "status": "real_but_test_interface_zeroed"
    }
  ],
  "round_win_rule": null,
  "rtp_integrity": {
    "paid_st": [
      140
    ],
    "fallback_warn": 0.5,
    "fallback_fail": 5.0
  }
}
