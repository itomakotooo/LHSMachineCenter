{
  "machine_id": "M274",
  "schema": "spintype-native/1",
  "modes": [
    1
  ],
  "inherits_from": null,
  "spin_types": {
    "140": {
      "role": "paid_spin",
      "play": "NormalCollectionSpin",
      "economy": {
        "win_field": "WinCredits",
        "kind": "real"
      },
      "signature": [
        "BetAmount",
        "CostCredits",
        "StopSymbolsByCol",
        "PayoutByPayline",
        "PayoutIdToWinAmount",
        "RewardLastNode",
        "ReelSkin",
        "CollectCount",
        "AccCredits",
        "CreditsSymbols",
        "WinCredits"
      ],
      "observed_fields": [
        "AccCredits",
        "BetAmount",
        "CollectCount",
        "CostCredits",
        "CreditsSymbols",
        "CurJackpotStoreWin",
        "GameplayTriggerType",
        "IsLackCreditsSpin",
        "LastCredits",
        "PayLineGroupId",
        "PayoutByPayline",
        "PayoutGroupId",
        "PayoutIdToWinAmount",
        "RTPId",
        "ReMarks",
        "ReelSkin",
        "RewardLastNode",
        "SpinTimes",
        "SpinType",
        "StopSymbolsByCol",
        "SymbolIndexToRewards",
        "WinCredits"
      ],
      "observed_field_presence": {
        "AccCredits": 1.0,
        "BetAmount": 1.0,
        "CollectCount": 1.0,
        "CostCredits": 1.0,
        "CreditsSymbols": 1.0,
        "CurJackpotStoreWin": 1.0,
        "GameplayTriggerType": 0.1429,
        "IsLackCreditsSpin": 1.0,
        "LastCredits": 1.0,
        "PayLineGroupId": 1.0,
        "PayoutByPayline": 1.0,
        "PayoutGroupId": 1.0,
        "PayoutIdToWinAmount": 1.0,
        "RTPId": 1.0,
        "ReMarks": 1.0,
        "ReelSkin": 1.0,
        "RewardLastNode": 1.0,
        "SpinTimes": 1.0,
        "SpinType": 1.0,
        "StopSymbolsByCol": 1.0,
        "SymbolIndexToRewards": 1.0,
        "WinCredits": 1.0
      },
      "observed_fields_source": "st_inventory M274/mode_1 (56 v3 chunks, 3 config buckets d35327a7/847d89cd/874f14ee, code_md5 9633e9d6, 2026-06-18; ST140 2,240,000 rows). SAME NormalCollectionSpin signature as M279/M277/M278 ST140 (CollectCount/AccCredits/CreditsSymbols/SymbolIndexToRewards). Economy invariant: per-bucket sum(WinCredits)==server FeatureWin[NormalCollectionSpin] EXACT (644,406,500/644,406,500 on bucket d35327a7, 320 robots). NOTE: CollectCount is flat 0 on this cached data (the collect metronome is dormant on this config/skin; SymbolIndexToRewards never populated -> NOT a live collect-coin channel). pid 5801 carried on 22,784/22,784 win==0 = the ListRewardWheel trigger marker (pays nothing)."
    },
    "139": {
      "role": "hold_respin",
      "play": "ListRewardWheel",
      "economy": {
        "win_field": "WinCredits",
        "kind": "real"
      },
      "signature": [
        "BetAmount",
        "CostCredits",
        "ReMarks",
        "PayoutIdToWinAmount",
        "ReelSkin",
        "WinCredits"
      ],
      "observed_fields": [
        "BetAmount",
        "CostCredits",
        "CurJackpotStoreWin",
        "IsLackCreditsSpin",
        "LastCredits",
        "PayLineGroupId",
        "PayoutByPayline",
        "PayoutGroupId",
        "PayoutIdToWinAmount",
        "RTPId",
        "ReMarks",
        "ReelSkin",
        "RewardLastNode",
        "SpinTimes",
        "SpinType",
        "StopSymbolsByCol",
        "WinCredits"
      ],
      "observed_field_presence": {
        "BetAmount": 1.0,
        "CostCredits": 1.0,
        "CurJackpotStoreWin": 1.0,
        "GameplayTriggerType": 0.1386,
        "IsLackCreditsSpin": 1.0,
        "LastCredits": 1.0,
        "PayLineGroupId": 1.0,
        "PayoutByPayline": 1.0,
        "PayoutGroupId": 1.0,
        "PayoutIdToWinAmount": 1.0,
        "RTPId": 1.0,
        "ReMarks": 1.0,
        "ReelSkin": 1.0,
        "RewardLastNode": 1.0,
        "SpinTimes": 1.0,
        "SpinType": 1.0,
        "StopSymbolsByCol": 1.0,
        "WinCredits": 1.0
      },
      "observed_fields_source": "st_inventory M274/mode_1 (ST139 230,525 rows). FREE granted hold-and-respin board collect (cost=0 on all rows; StopSymbolsByCol/PayoutByPayline/RewardLastNode present-as-key but EMPTY [] -- NOT a real reel spin). ReMarks='Minigame CellIndexes: ...; curTimes:N; totalTimes:M; RewardWheelIds; RewardCellIndexes; AddLock:[r:c]; TotalLock:[...]' on 11,595/11,595 -- a locking board respin (AddLock/TotalLock GROW within a burst, curTimes/totalTimes = respin counter). Win-bearing PER ROUND (5,414/11,595 win>0) with NO Settle field -> NOT a single-settle minigame; structurally a hold-and-respin block (M277 LockReSpin precedent, win-bearing per round). NO natural payid (PayoutIdToWinAmount {} on 100%) -> attributed by the m274_listrewardwheel_settlement SynthesizePayIdRule. Economy invariant: per-bucket sum(WinCredits)==server FeatureWin[ListRewardWheel] EXACT (767,515,000/767,515,000 on bucket d35327a7).",
      "trigger_paths": {
        "fallback": {
          "kind": "trigger_anchor_walk",
          "note": "NO usable round_field discriminator on M274 ST139: GameplayTriggerType is absent on this bucket's ST139 rows (optional feature field, 13.86% mode-wide) and is NOT a per-round trigger discriminator; RewardLastNode/StopSymbolsByCol/PayoutByPayline are empty on ST139 (the board lives in ReMarks). The trigger lives on the OPENING paid ST140 (round_ctx.last_paid_round), so the extractor uses the trigger_anchor_walk fallback. Anchor reads the opening ST140: listrewardwheel_random = pid '5801' present in PayoutIdToWinAmount with win==0 (22,784/22,784 win==0, VERIFIED fleet-wide). All 16,267 ST139 bursts (bucket d35327a7, 320 robots) opened by pid 5801 win==0; 0 opened by CollectCount peak (CollectCount flat 0 on this data -> the collect_peak pity path is DORMANT, omitted -- declare-on-data, never by assumption); 0 orphan bursts."
        },
        "paths": {
          "listrewardwheel_random": {
            "opened_by": {
              "payout_id": "5801"
            },
            "label": "ListRewardWheel trigger (random)"
          }
        },
        "multi_trigger_policy": "additive_sessions"
      }
    }
  },
  "trigger": {
    "payout_id": null,
    "remarks": "Minigame",
    "opens": "ListRewardWheel",
    "note": "No single round-level trigger pid: ST139 ListRewardWheel (CostCredits=0, free granted hold-and-respin board collect with a locking cell board -- AddLock/TotalLock grow, curTimes/totalTimes respin counter, RewardWheelIds/RewardCellIndexes cell prizes) is opened off the opening paid ST140 by ONE observed path on this data -- listrewardwheel_random: the opening ST140 carries pid '5801' in PayoutIdToWinAmount with WIN==0 (a trigger MARKER that pays nothing; 22,784/22,784 win==0). 16,267 bursts on bucket d35327a7, all 5801-opened, 0 CollectCount-peak (the collect metronome is FLAT 0 on this cached config -- the BuffCollection-peak pity path the legacy bcm_cycle_anchor_m274 rule targeted is DORMANT here; declared-on-data, not assumed). ST139 is win-bearing PER ROUND (NO Settle field) and carries NO natural payid (PayoutIdToWinAmount {} on 100%) -> attributed by the m274_listrewardwheel_settlement SynthesizePayIdRule (EXACT M278 ST160 / M43 ST51 precedent). The trigger_paths block uses the trigger_anchor_walk fallback (M277/M278 precedent), not a round_field discriminator."
  },
  "validation": {
    "status": "confirmed",
    "user_signed_off": true,
    "confirmed_against": {
      "mode": 1,
      "rawdata": "M274/mode_1"
    },
    "evidence": "Onboard re-run (fleet T2) 2026-06-18 on 56 real v3 chunks (provenance CLEAN: _cache_version=3 on 56/56, _dev_sample absent; 3 config_md5 buckets d35327a7[40]/847d89cd[8]/874f14ee[8], single code_md5 9633e9d6 -- mixed-config dir, structure work value-agnostic across all real chunks, served reports go per-bucket per M279 precedent). Only TWO STs present in EVERY bucket: ST140 (paid NormalCollectionSpin, cost=1000) + ST139 (free ListRewardWheel, cost=0). ST140 = SAME NormalCollectionSpin signature as confirmed M279/M277/M278 (STRICT-REUSE the 4 PER_SPINTYPE plugins + collect_mechanic). ST139 = NEW signature (no LockLines [M277], no Settle/LockReels/AddExtraRatio/PlayType [M278]; lock state lives in ReMarks AddLock/TotalLock) -> NEW EVENT assembled with ZERO new plugin via the committed hold_respin role (ROLE_ANALYSES['hold_respin']=freespin_dynamics, family-resolved) + the generic trigger_path anchor-walk extractor + a config-only SynthesizePayIdRule. ABORT checks CLEAR: ST140 SymbolIndexToRewards never populated (NOT a live collect-coin channel); ST140 additive-residual 0 (no _unattributed_residual: win never exceeds sum(payid) alongside a payline); ST139 NO Settle-chain the shared plugin rejects. Economy invariant HOLDS EXACT per bucket: ST140 644,406,500==server FeatureWin[NormalCollectionSpin]; ST139 767,515,000==server FeatureWin[ListRewardWheel] (bucket d35327a7, 320 robots). play names = server analysisResult.FeatureWin keys (NormalCollectionSpin / ListRewardWheel), case-exact.",
    "caveats": [
      "ROLE TOKEN: 'hold_respin' is REUSED verbatim from the COMMITTED machine_spec.py (KNOWN_ROLES, FREESPIN_FAMILY_ROLES={freespin,hold_respin}, ROLE_ANALYSES['hold_respin']=('freespin_dynamics',)). NO machine_spec.py edit, NO base_hash flip. M274 makes hold_respin a SHARED token (M277+M278+M274) -- the intended family-additive design; all three want the SAME freespin_dynamics analysis so the ROLE hook cannot mis-fire onto an unrelated mechanic. play 'ListRewardWheel' is M274-exclusive in the fleet.",
      "ATTRIBUTION: ST140 native PayoutId space (real symbol pids 6/7/... + pid '5801' = the 0-win ListRewardWheel trigger MARKER, 22,784/22,784 win==0, carried not dropped). ST139 NO natural payid (PayoutIdToWinAmount {} on 230,525/230,525) -> the config-only m274_listrewardwheel_settlement SynthesizePayIdRule (spin_types=[139], label_format=spin_type) mints a single real pid 'st139' for the whole ST139 win (EXACT M278 ST160 / M43 ST51 precedent). NO machine_round_win_rules.json closure change.",
      "LEGACY RULE: configs/machine_round_win_rules.json already carries 'bcm_cycle_anchor_m274' (a 2026-05-12 flat-manifest-era bcm_cycle_anchor rule targeting a CollectCount==1000 ListRewardMinigame trigger). On THIS cached data CollectCount is flat 0 (the metronome is dormant) so that rule never fires (it only anchors at CC==1000) -- it is harmless and additive; the SpinType-native path attributes ST139 via the new synthesize_pay_id rule + the 5801 anchor-walk path. The legacy rule is LEFT UNTOUCHED (not a closure file).",
      "TRIGGER-PATH discriminator: M274 ST139 uses the trigger_anchor_walk FALLBACK (M277/M278 precedent), NOT a round_field discriminator (no usable per-round discriminator on ST139). One observed path on this data: listrewardwheel_random (pid 5801 win==0). The collect_peak pity path is DORMANT (CollectCount flat 0) -> NOT declared (declare-on-data; extend only when a future config/skin populates CollectCount).",
      "parser_blind (framework-team, NEVER fabricated into the manifest): (a) ListRewardWheel per-round CELL board map -- the ReMarks 'CellIndexes' / 'RewardWheelIds' / 'RewardCellIndexes' lists + the AddLock/TotalLock lock-growth + curTimes/totalTimes respin counter need a per-round ReMarks accumulator scoped to ST139 (the parser keeps only 3 sample ReMarks/ST) -- same boundary as M279 wheel CellIndex + M277 LockReSpin held-wild ladder; (b) per-burst respin RUN-LENGTH / settled-multiplier value-build curve needs per-burst run-length accumulation (parser, base-closure) -- band-level per-path multiplier IS delivered by freespin_dynamics, the length axis is parser_blind (DIRECTION §3).",
      "collect_mechanic cosmetics (fleet-wide, M275/M279/M278/M277 precedent): on this data CollectCount is flat 0 so no cycle is observed; estimated_correction_pp (the RTP-bearing output) is 0.0 and correct. Framework-team precision item, not an RTP error.",
      "modes: [1] only -- the only mode with cached rawdata. Extend on data, never by assumption (the M43/M279/M275/M278 cross-mode pattern).",
      "MIXED-MD5 dir: 3 config buckets (d35327a7[40]/847d89cd[8]/874f14ee[8]), single code_md5. Structure claims are value-agnostic across all real chunks; every NUMBER above belongs to bucket d35327a7. Any SERVED report MUST go through the md5-scoped generate path and be stamped with the source-chunk md5 (M279 mixed-dir precedent).",
      "RTP magnitude is not an onboarding criterion (parse-correctness is); no RTP value is pinned. Per feedback_dev_ci_scope.md, prod-side re-sample governs the production RTP read."
    ],
    "date": "2026-06-18",
    "signed_off_date": "2026-06-18",
    "gate8_note": "fleet T1/T2 batch gate-8 sign-off 2026-06-18: no out-of-engine mechanics declared beyond the fleet CurJackpotStoreWin test-interface-zeroing (and M90's gift, recorded). Objective gates 1-7 pass; mechanical independent-recompute breaker held."
  },
  "out_of_engine_mechanics": [
    {
      "name": "CurJackpotStoreWin progressive (test-interface-zeroed)",
      "desc": "Field 'CurJackpotStoreWin' present on every ST140/ST139 round but =0 on 100% of in-sample records. The fleet-wide pattern (user-confirmed 2026-06-12 on M275, gate-8; M277/M278/M279/M283 precedent): a real progressive jackpot store that the CURRENT testspin interface forces to 0; the sample correctly shows 0. Parse as-is with the progressive contributing 0; the field is already in both ST signatures so parsing is FORWARD-COMPATIBLE -- when the interface populates it the parser picks it up unchanged. The reported RTP is the BASE RTP (excludes the progressive). Declaration-based out-of-engine protocol (gate-8): nothing else is declared for M274, so nothing else is presumed hidden -- the single gate-8 item to surface to the user.",
      "rtp_impact": true,
      "rtp_impact_current": 0,
      "source": "domain",
      "status": "real_but_test_interface_zeroed"
    }
  ],
  "round_win_rule": "m274_listrewardwheel_settlement (synthesize_pay_id, spin_types=[139], label_format=spin_type) -- added to configs/machine_round_win_rules.json; zeroes _unattributed_st139 (ST139 ListRewardWheel win-bearing per round with NO PayoutIdToWinAmount -> minted to single real pid 'st139'). EXACT M278 ST160 / M43 ST51 precedent. The legacy flat-era 'bcm_cycle_anchor_m274' rule is left untouched (dormant on this data; CollectCount flat 0).",
  "rtp_integrity": {
    "paid_st": [
      140
    ],
    "fallback_warn": 0.5,
    "fallback_fail": 5.0
  }
}