{
  "machine_id": "M66",
  "schema": "spintype-native/1",
  "modes": [
    1
  ],
  "inherits_from": null,
  "spin_types": {
    "1": {
      "role": "paid_spin",
      "play": "Normal",
      "economy": {
        "win_field": "WinCredits",
        "kind": "real"
      },
      "signature": [
        "BetAmount",
        "CostCredits",
        "StopSymbolsByCol",
        "PayoutByPayline",
        "WinCredits"
      ],
      "observed_fields": [
        "BetAmount",
        "CostCredits",
        "CurJackpotStoreWin",
        "GameplayTriggerType",
        "IsLackCreditsSpin",
        "LastCredits",
        "PayLineGroupId",
        "PayoutByPayline",
        "PayoutGroupId",
        "PayoutIdToWinAmount",
        "RTPId",
        "ReMarks",
        "ReelSkin",
        "RewardLastNode",
        "SpinTimes",
        "SpinType",
        "StopSymbolsByCol",
        "WinCredits"
      ],
      "observed_field_presence": {
        "BetAmount": 1.0,
        "CostCredits": 1.0,
        "CurJackpotStoreWin": 1.0,
        "GameplayTriggerType": 1.0,
        "IsLackCreditsSpin": 1.0,
        "LastCredits": 1.0,
        "PayLineGroupId": 1.0,
        "PayoutByPayline": 1.0,
        "PayoutGroupId": 1.0,
        "PayoutIdToWinAmount": 1.0,
        "RTPId": 1.0,
        "ReMarks": 1.0,
        "ReelSkin": 1.0,
        "RewardLastNode": 1.0,
        "SpinTimes": 1.0,
        "SpinType": 1.0,
        "StopSymbolsByCol": 1.0,
        "WinCredits": 1.0
      },
      "observed_fields_source": "st_inventory M66/mode_1 bucket 6b7cc2ce/536fc5a2 2026-06-18"
    }
  },
  "trigger": {
    "payout_id": null,
    "remarks": null,
    "opens": null,
    "note": "Base-game-only machine in mode 1. No granted-session / feature trigger exists: ReMarks empty on 40,000/40,000 rounds, GameplayTriggerType constant 0, FeatureWin single key 'Normal'. No trigger session structure to reconstruct. NO trigger_paths dimension (single ST, no discriminator)."
  },
  "validation": {
    "status": "auto",
    "user_signed_off": false,
    "confirmed_against": {
      "mode": 1,
      "rawdata": "M66/mode_1"
    },
    "evidence": "T1-batch single-ST paid onboarding (manifest-only, NO new code; strict mirror of confirmed M70). W1 deep-parse of the real cache (rawdata/M66/mode_1/chunk_0002.json, _cache_version 3 REAL, _machine M66 _mode 1 _bet 1000, single config-md5 bucket 6b7cc2ce/536fc5a2, 40,000 rounds): st_inventory SpinType tally {1:40000} (exactly ONE ST); st1 role paid_spin (CostCredits>0 on 40,000/40,000, BetAmount=CostCredits=1000, StopSymbolsByCol + PayoutByPayline + PayoutIdToWinAmount present 100%); ReMarks empty on 40,000/40,000; GameplayTriggerType constant 0; analysisResult FeatureWin single key 'Normal' across all 8 robots (json-decoded twice). Field set is byte-identical to confirmed M70 (no extra field, none missing, all observed_field_presence 1.0). pay_id native space: Sigma(PayoutIdToWinAmount)==Sigma(WinCredits) EXACTLY (16,852,000==16,852,000) over the 40,000-round sample, zero mismatch rounds -> no no-payid settlement, no SynthesizePayIdRule needed. Reuse: st1 STRICT-REUSE of the shared paid_spin plugins (role already in KNOWN_ROLES, no extension, no role/play hook, no cross-fire). report_engine.generate_report_from_chunks('M66',1,bet=1000): rtp_integrity_check.passed=True, fallback_buckets_found=[] (NO _unattributed_* bucket, share 0), manifest loads. base_hash 3ddaa183f38c UNCHANGED before/after (manifest base-excluded). NO new plugin, NO fork, NO closure touch.",
    "caveats": [
      "Single config-md5 bucket 6b7cc2ce/536fc5a2 (single chunk chunk_0002.json, 40,000 rounds; no mixed-md5 pooling concern).",
      "GameplayTriggerType is present and constant 0 on all 40,000 rounds in mode 1 (NOT in the analyzer baseline field set; this machine emits it). Non-zero meaning undetermined from mode-1 data (possible trigger-type discriminator in other modes). Parse-as-is (constant 0); forward-compatible (in the st1 signature).",
      "Modes 2/5/7 NOT analyzed (mode-1-only onboarding). They may exercise other STs / non-zero GameplayTriggerType and MUST get their own W1 + reuse pass before any reuse claim. validation.status stays 'auto' until breaker + gate-7 render verify + user sign-off.",
      "RTP value is intentionally NOT pinned as a gate (value-agnostic onboarding). The deliverable is the report-generating STRUCTURE; integrity is asserted via rtp_integrity_check.passed + zero fallback + payid==wincredits invariant, not an RTP magnitude."
    ],
    "date": "2026-06-18"
  },
  "out_of_engine_mechanics": [
    {
      "name": "CurJackpotStoreWin progressive (test-interface-zeroed)",
      "desc": "Field 'CurJackpotStoreWin' is present on every st1 round but =0 on 100% of 40,000 records. Established fleet-wide test-interface zeroing, declaration-confirmed (memory: gate-8 ruling + M279/M283 sign-off - a real progressive jackpot store the current testspin interface forces to 0). Parse as-is at 0; the field is already in the st1 signature so parsing is FORWARD-COMPATIBLE (when the interface populates it the existing parser picks it up unchanged). Reported RTP excludes the progressive; it would add on top once the interface enables it. NOT a per-machine investigation - fleet pattern, one-time recorded.",
      "rtp_impact": true,
      "rtp_impact_current": 0,
      "source": "domain",
      "status": "real_but_test_interface_zeroed"
    }
  ],
  "rtp_integrity": {
    "paid_st": [
      1
    ],
    "fallback_warn": 0.5,
    "fallback_fail": 5.0
  }
}
